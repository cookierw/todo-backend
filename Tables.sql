CREATE TABLE users (id VARCHAR(100) PRIMARY KEY, username VARCHAR(20) NOT NULL UNIQUE, password VARCHAR(30) NOT NULL);
CREATE TABLE todos (id VARCHAR(100) PRIMARY KEY, user_id VARCHAR(100) NOT NULL CONSTRAINT fk_user REFERENCES users(id), body VARCHAR(255) NOT NULL, complete BOOLEAN NOT NULL);